// AOS ANIMATE
AOS.init();


// Obtiene la URL actual
// var currentUrl = window.location.href;
// Selecciona los enlaces de navegación
// var navLinks = document.querySelectorAll('.custom-nav-link');
// Itera a través de los enlaces y agrega la clase 'active' al enlace correspondiente a la página actual
// for (var i = 0; i < navLinks.length; i++) {
//     var linkHref = navLinks[i].getAttribute('href');
//     if (currentUrl.endsWith(linkHref)) {
//         navLinks[i].classList.add('active');
//     }
// }